# Scoreboard

## Install

```sh
# install bundler if not available
gem install bundler

# install gem dependencies
bundle install

# start server
rails s
```

## Testing

```sh
bundler exec rspec
```
